name: Service CI

on:
  push:
    branches: [develop, main]
  pull_request:
    branches: [develop, main]

jobs:
  ci:
    uses: SkillCareer/skill-service/.github/workflows/reusable-ci.yml@main
    with:
      node_version: "18"
      image_name: "skill-service"
    secrets: inherit
